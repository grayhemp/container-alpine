language: bash
services:
  - docker
env:
  global:
    - DOCKER_REGISTRY_USER='grayhemp'
script:
  - make
deploy:
  provider: script
  script:
    - echo "${DOCKER_REGISTRY_PASSWORD?}" | make push
  on:
    branch: master
