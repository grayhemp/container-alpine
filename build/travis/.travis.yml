language: bash
services:
  - docker
env:
  global:
    - REGISTRY_USER='grayhemp'
script:
  - make
deploy:
  provider: script
  script:
    - echo "${REGISTRY_PASSWORD?}" | make push
  on:
    branch: master
